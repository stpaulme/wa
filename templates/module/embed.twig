<div class="module embed">

    {% if module.heading %}
        <h2 class="embed-heading">{{module.heading}}</h2>
    {% endif %}

    <div class="embed-container">
        {{module.embed}}
    </div>

    <style>
        {% if 'youtube.com' in module.embed %}
        .embed-container { position: relative; padding-bottom: 56.25%; height: 0; overflow: hidden; max-width: 100%; } .embed-container iframe, .embed-container object, .embed-container embed { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }
        {% endif %}

        {% if 'vimeo.com' in module.embed %}
        .embed-container { position: relative; padding-bottom: 56.25%; height: 0; overflow: hidden; max-width: 100%; } .embed-container iframe, .embed-container object, .embed-container embed { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }
        {% endif %}

        {% if 'dailymotion.com' in module.embed %}
        .embed-container { position: relative; padding-bottom: 56.25%; height: 0; overflow: hidden; max-width: 100%; } .embed-container iframe, .embed-container object, .embed-container embed { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }
        {% endif %}

        {% if 'google.com' in module.embed %}
        .embed-container { position: relative; padding-bottom: 56.25%; height: 0; overflow: hidden; max-width: 100%; } .embed-container iframe, .embed-container object, .embed-container embed { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }
        {% endif %}

        {% if 'gettyimages.com' in module.embed %}
        .embed-container { position: relative; padding-bottom: 79.96632996632997%; height: 0; overflow: hidden; max-width: 100%; } .embed-container iframe, .embed-container object, .embed-container embed { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }
        {% endif %}
    </style>

</div>