<div class="module gallery">

    {% if module.heading %}
    <div class="row">
        <div class="col">
           <h2 class="h4">{{module.heading}}</h2>
        </div>
    </div>
    {% endif %}

    <div class="row">
        {% for image in module.gallery %}
        {% set image = TimberImage(image) %}

        <div class="col-md-4">
            <a href="{{image.src}}"><img class="mw-100" src="{{image.src|resize(800)}}" alt="{{image.alt}}" title="{{image.title}}"></a>
        </div>

        {% endfor %}      
    </div>
</div>