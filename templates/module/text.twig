<div class="module text">
    <div class="container">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                {% if module.heading %}
                <h2 class="h4">{{module.heading}}</h2>
                {% endif %}
                
                {% if module.copy %}
                {{module.copy}}
                {% endif %}
            </div>
        </div>
    </div>
</div>